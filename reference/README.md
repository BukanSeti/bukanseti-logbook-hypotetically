# Layout reference

Place the authorized International Coradine Rev 3 reference photographs or scans in a private working directory, not in this public repository unless you hold distribution rights.

The current workbook renderer reproduces the required semantic structure, grouped headers, border grid, print areas, manual page breaks, and A3 landscape output. Pixel-level matching requires the actual reference pages and a dedicated template-calibration pass.
