FROM python:3.12-slim

WORKDIR /app
COPY pyproject.toml README.md ./
COPY src ./src
RUN pip install --no-cache-dir ".[reference-api]"

ENV PORT=8080
CMD ["sh", "-c", "uvicorn international_coradine.reference_api.app:app_factory --factory --host 0.0.0.0 --port ${PORT:-8080} --proxy-headers"]
